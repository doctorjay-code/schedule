# 가계부 웹 연동 로드맵

> **작성일:** 2026-08-20  
> **현재 우선순위:** `현금`·`기업`·`토스` 시트를 웹 가계부에 먼저 연결한다. `잔액전망`의 웹 구현과 시트 단순화는 이 연결이 검증된 뒤 진행한다.

## 1. 확정한 원칙

가계부의 **원본 데이터는 Google Sheets**로 관리한다. 웹은 원본을 중복 보관하지 않고, 시트를 읽어 입력·조회·분석 화면을 제공한다. 브라우저 저장소와 현재의 로컬 시드 데이터는 전환 과정의 안전망으로만 유지하며, 시트 연결 검증 후 기준 데이터로 사용하지 않는다.

| 시트 | 원본 역할 | 웹에서의 역할 |
|---|---|---|
| `기업` | 기업카드 개별 사용 내역 | 기업카드 거래 목록·월별 통계·입력/수정 |
| `토스` | 토스카드·토스은행 가계부 내역 | 토스 거래 목록·월별 통계·입력/수정 |
| `현금` | 현금 사용 내역 | 현금 거래 목록·입력/수정 |
| `계좌흐름` | 실제·예정 통장 입출금 | 이후 잔액전망 계산의 유일한 현금 잔액 기준 |
| `코드표` | 드롭다운 기준값 | 웹 입력 화면의 선택 목록 |

## 2. 현재 먼저 진행할 범위

웹은 기존 가계부 메뉴와 화면을 유지한 채, `기업`·`토스`·`현금` 시트의 데이터를 읽어 현재 거래 목록과 월별 통계를 표시한다. 최초 단계에서는 **읽기 연동을 먼저 검증**하고, 데이터 건수·날짜·수입/지출·사용자·사용처·항목·비고·고정비 여부가 시트와 정확히 일치하는지 확인한다.

그 다음 단계에서만 웹의 거래 추가·수정·삭제를 각 시트에 반영한다. 토스카드 거래는 `토스` 시트만, 기업카드 개별 사용은 `기업` 시트만 수정한다. 현금 거래는 `현금` 시트만 수정한다.

## 3. 보류: 잔액전망의 웹 구현

현재 `잔액전망` 시트는 검산용으로 유지한다. 지금의 월별 요약행, 행 그룹, 카드 결제 상세 그룹, 잔액 수식은 **아직 삭제하거나 단순화하지 않는다.** 웹에서 같은 결과를 재현하고 월별 금액이 일치하는 것을 확인한 뒤에만 정리한다.

웹의 잔액전망은 다음 규칙으로 구현한다.

| 기능 | 웹 구현 규칙 |
|---|---|
| 실제 현금 잔액 | `계좌흐름`만 기준으로 토스은행·기업은행·현금 합계를 계산 |
| 가계부 상세 | `기업`·`토스`·`현금`으로 사용자·사용처·항목·비고를 보완 표시 |
| 토스 월별 요약 | 지난달은 월말 기준, 현재월은 오늘 기준으로 웹에서 표시 |
| 미래 예정 거래 | `계좌흐름`의 예정 행을 오늘 이후 날짜순으로 표시 |
| 기업카드 예정 결제 | `기업` 사용내역의 전월 13일~당월 12일을 합산하여 결제일 카드로 표시 |
| 실제 카드대금 출금·선결제 | `계좌흐름`의 실제 출금 행만 현금 잔액에 반영 |
| 상세 보기 | 결제 행을 누르면 해당 결제주기 기업카드 사용내역을 펼쳐 표시 |

중요하게, 토스·기업의 가계부 거래와 계좌흐름의 실제 출금은 서로 중복 집계하지 않는다. 웹의 잔액 계산은 계좌흐름만 사용한다.

## 4. 기존 연동 조사 결과

2026-08-20에 기존 일정 동기화용 웹 엔드포인트를 확인했다. 기본 요청과 `action=GET_LEDGER_DATA` 요청은 모두 일정 목록을 반환하며, 현재 배포본에는 가계부 데이터 요청이 구현되어 있지 않았다. 이후 스프레드시트 소유 계정으로 로그인해 기존 Apps Script 프로젝트 `진료`의 원본을 찾았다. 이 원본은 `doGet(e)`에서 `action` 값을 분기하고 `GET_COLORS`를 처리하는 구조이므로, 일정 기능을 건드리지 않고 `GET_LEDGER_DATA` 같은 가계부 읽기 전용 요청을 추가할 수 있다. 원본은 단일 `Code.gs` 파일(163줄)이며, 기존 일정 데이터를 갱신하는 `doPost`와 시트 서식을 적용하는 함수도 포함한다. 기존 일정용 스크립트와 운영원본 시트에는 어떠한 코드·열 수정도 적용하지 않았다. 수정 전 일정용 스크립트는 Apps Script 프로젝트 기록에도 복구 지점이 남아 있다. 2026-08-20에는 가계부 전용 스크립트 프로젝트를 새로 만들었으며, 이 프로젝트는 아직 기본 `myFunction`만 있는 초기 상태이다.

## 5. 구현 순서

1. 가계부 전용 Apps Script 프로젝트 `Schedule 가계부 연동`(프로젝트 ID: `1evhGZNcJTkbvRz1EJAJOaQ5sFABeR8PSBVqtBrRX6GDFfJ51q3rh2e4x`)에 `기업`·`토스`·`현금` 읽기 요청을 추가한다. 일정용 `진료` 스크립트는 수정하지 않는다.
2. 웹에 `ledger-api.js`를 추가해 세 시트 데이터를 공통 거래 형식으로 변환한다.
3. 기존 가계부 목록과 월별 통계에 시트 데이터를 연결한다. **완료: 2026-08-20 읽기 전용 연결**
4. 시트 데이터와 웹 표시 건수·금액을 비교하고, 실패 시 현재 로컬 데이터로 안전하게 유지한다. **완료: 기업 114건·토스 534건·현금 88건 확인**
5. 거래 입력·수정·삭제의 시트 반영을 사용자 확인 후 추가한다.
6. 마지막으로 잔액전망을 웹에 구현하고, 시트의 잔액전망 탭을 raw 검산용으로 단순화할지 결정한다.

## 6. 관련 코드 예정 위치

| 파일 | 책임 |
|---|---|
| `js/services/ledger/ledger-api.js` | Apps Script를 통한 가계부 시트 읽기와 웹 거래 객체 정규화 |
| `js/features/ledger/ledger-app.js` | 현재 가계부 화면과 시트 데이터 연결, 실패 시 로컬 원본으로 복귀 |
| `js/features/ledger/transaction-view.js` | 날짜별 거래 카드의 고정비 태그 표시 |
| `js/features/ledger/forecast-engine.js` | 보류된 잔액전망의 계산 전용 모듈 |
| `js/features/ledger/forecast-view.js` | 보류된 잔액전망의 타임라인 화면 |

## 7. 작업 백업

현금·기업·토스 시트 연동 시작 전 웹 코드 백업은 다음 위치에 보관한다.

```text
backups/before-ledger-sheets-connection-20260820-070000/
```

## 8. 2026-08-20 1차 읽기 연결 검증

- 가계부 전용 Apps Script 웹 앱 `Schedule 가계부 연동` 버전 2를 배포했다. 엔드포인트는 `GET_LEDGER_DATA` 요청에서 **기업 114건, 토스 534건, 현금 88건**을 읽기 전용으로 반환한다.
- 웹 가계부는 세 탭 원본을 우선 표시하고, 읽기 실패 시 기존 로컬 원본을 사용하도록 처리했다. 이번 단계에서는 스프레드시트의 값·열·서식·행 그룹을 변경하지 않았다.
- 기업카드 주간 화면에서 2026-08-10~08-16의 기업 원본 거래와 `고정비` 태그를 확인했다. 태그는 날짜별 거래 카드의 오른쪽 위 빈 칸(기존 사용액 위치)에 표시된다.
- 현금내역 전체 화면에서 2026년 1~8월 합계 88건이 표시되는 것을 확인했다.
- 다음 단계: 웹에서 거래 추가·수정·삭제를 Google Sheets에 쓰는 기능은 사용자 확인 후 별도로 구현한다. 잔액전망 웹 구현은 이 연결이 안정된 뒤 마지막 단계에서 진행한다.

웹 검증 중 `＋ 거래 입력` 버튼은 시트 쓰기 연결 전까지 조회만 가능하다는 안내 문구와 함께 비활성화된 상태임을 확인했다. 시트 원본 행의 수정·삭제 버튼과 행 클릭 편집도 읽기 전용 단계에서는 열리지 않도록 처리했다.

## 9. 2026-08-20 양방향 시트 동기화 완료

가계부 전용 Apps Script 웹 앱을 버전 3으로 갱신해 `GET_LEDGER_DATA` 외에 거래 추가·수정·삭제 요청을 지원하도록 확장했다. 대상은 `기업`·`토스`·`현금` 세 탭으로 한정하며, 시트의 열·서식·행 그룹은 변경하지 않는다. 웹에서 새 거래를 저장하면 선택한 탭의 마지막 행에 추가되고, 수정은 id를 우선으로 원본 행을 찾아 반영하며, 삭제는 확인 후 원본 행을 삭제한다. 각 성공 동작 뒤에는 시트 원본을 다시 읽어 웹에 재표시한다.

검증은 API 수준과 웹 화면 수준에서 모두 진행했다. API 수준에서는 현금 탭에 검증 거래를 추가·수정·삭제해 잔여 행이 없는 것을 확인했다. 웹 화면에서도 토스 탭에 검증 거래를 추가하고 비고를 수정한 뒤 삭제했으며, 최종적으로 웹 목록과 토스 운영원본 모두에서 해당 거래가 사라진 것을 확인했다. 검증용 거래는 운영 데이터에 남아 있지 않다.

현재 웹의 `＋ 거래 입력`, 각 행의 `수정`, `삭제`, `최신 내역 반영`은 Google Sheets 원본과 양방향으로 동작한다. 다만 공개 링크 방식으로 배포한 웹 앱은 주소를 아는 사람이 쓰기 요청을 보낼 수 있으므로, 배포 URL과 웹 소스를 외부에 공유하지 않는다. 잔액전망의 웹 구현은 이 양방향 연결이 안정된 후 마지막 단계에서 계속한다.

추가 검증으로 기업 시트에도 1원 검증 거래를 추가·수정·삭제한 뒤 GET 재조회에서 잔여 행이 없음을 확인했다. 따라서 기업·토스·현금 모두 동일한 양방향 저장 경로를 사용한다.

양방향 저장 대상과 웹 입력 선택지를 일치시키기 위해 결제수단 입력에서 `기타`를 제거했다. 이제 새 거래는 기업카드·토스카드·현금 중 하나의 운영원본 시트에만 저장된다.

최신 내역 반영을 눌러 시트 원본을 다시 읽은 뒤 웹 입력 버튼과 수정·삭제 버튼이 정상 활성화되는 것을 재확인했다.

최종 웹 화면 검증에서 새 거래 입력 모달의 결제수단은 토스·기업·현금 세 가지로만 표시됐고, 저장 없이 모달을 닫아 추가 원본 데이터는 만들지 않았다.


## 10. 2026-08-20 가계부 조회 성능 최적화

웹 시작 시 일정 화면을 유지한 채 가계부 기능을 선로딩하도록 변경했다. 따라서 기업·토스·현금 세 시트는 가계부를 처음 누르기 전에 백그라운드로 읽기 시작하며, 초기 조회가 끝난 뒤 가계부·기업카드·현금내역 탭을 열면 즉시 기존 데이터를 표시한다.

가계부의 상단 새로고침과 하단 `최신 내역 반영`은 현재 보고 있는 원본만 다시 읽도록 변경했다. 토스카드에서는 `토스`, 기업카드에서는 `기업`, 현금내역에서는 `현금`만 API에 요청한다. 자금흐름은 가계부 원본 탭이 아니므로 시트 읽기 요청을 하지 않는다.

Apps Script `Schedule 가계부 연동`은 버전 4로 배포했다. 기존 웹 앱 URL은 유지했다. API 검증에서 전체 조회는 14.591초, 토스 단일 조회는 6.390초, 현금 단일 조회는 6.145초, 기업 단일 조회는 6.902초였고 각 단일 조회는 요청한 탭 하나만 반환했다.

현금 내역을 새로고침한 뒤 기업카드 화면으로 바뀌는 기존 렌더링 문제를 발견해 보완했다. 이제 현금·자금흐름 화면에서 새로고침을 마쳐도 현재 화면과 기간을 유지한다. 웹에서 토스·기업·현금 탭의 초기 표시, 토스·현금 단일 재조회 시 로딩→완료 문구 전환, 현금 화면 유지까지 확인했다.


## 11. 2026-08-21 마지막 시트 동기화본 캐시 통일

일정과 가계부는 이제 브라우저에 마지막으로 **Google Sheets에서 정상 수신한 결과만** 임시 보관한다. 다음에 웹을 열면 이 동기화본을 바로 화면에 표시하고, 동시에 뒤에서 최신 Google Sheets 값을 다시 읽어 성공했을 때만 화면과 동기화본을 교체한다.

가계부의 정적 거래 seed와 기존 `schedule_ledger_transactions_v1` 임시 거래는 더 이상 초기 화면 데이터로 사용하지 않는다. 일정 역시 기존 일반 로컬 일정값 대신 `schedule_last_sheet_snapshot_v1`의 마지막 성공 시트 동기화본만 초기 표시한다. 캐시가 한 번도 없는 첫 실행에서는 각각 `일정 불러오는 중…`, `가계부 불러오는 중…` 안내를 표시한다. 시트 연결에 실패해도 마지막 성공 시트 동기화본이 있으면 그대로 유지하고 연결 안내를 표시한다.

웹 재실행 뒤 일정·가계부 동기화본의 즉시 표시, 일정·가계부 수동 새로고침의 로딩→완료 상태 전환을 확인했다. 가계부는 계속 웹 시작 시 세 원본 전체를 백그라운드 조회하고, 이후 수동 새로고침은 현재 원본 탭 하나만 조회한다. 자금흐름은 가계부 원본 탭이 아니므로 기존 자금흐름 데이터 경로를 유지한다.


## 12. 2026-08-21 사용자 직접입력 열 제거 및 비고 기반 자동 분류

- 기업·토스·현금 시트의 `사용자_직접입력` H열을 모두 삭제해 공통 열 구조를 `G: 사용자, H: 사용처, I: 항목, J: 비고`로 단순화했다.
- 사용자(G열)는 세 시트 모두 `비고(J열)`에서 `콩콩|쥬쥬|지니`를 추출하고, 이름이 없으면 `기타`를 반환하는 배열 수식으로 통일했다.
- 현금 기존 88건의 자동 분류 결과는 콩콩 37건, 지니 21건, 쥬쥬 19건, 기타 11건으로 확인했다.
- 사용처 드롭다운은 기업·토스·현금 모두 식비·교통·문화·생활·보험·이자·상환·저축·입금·기타 10개 항목으로 통일했다.
- 웹 가계부는 사용자 직접 선택을 제거하고 시트의 비고 기반 사용자 열만 읽도록 변경했다. 웹 입력 모달과 필터에도 같은 10개 사용처를 반영했다.
- 가계부 Apps Script 공개 웹 앱은 버전 5로 배포했으며 기존 URL을 유지했다. 현금 단일 조회 API는 삭제된 직접입력 헤더 없이 사용자·사용처·항목·비고 헤더를 정상 반환하는 것을 확인했다.
- 백업: Drive 스프레드시트 사본 `1JTRQYUkykDVOBuiyGy2HWuE4guDtktCvp0cegYG5qJY`, 프로젝트 폴더 `backups/before-remove-direct-user-column-20260821-101052/`.
- 수정 처리에서 G열 배열 수식 결과를 일반 텍스트로 덮어쓰지 않도록 Apps Script를 보완했다. 기존 거래 수정은 **A:F와 H:N만 갱신**하고 G열 `사용자`는 건너뛴다. 이 보완을 포함한 공개 웹 앱 **버전 6**을 기존 URL 그대로 배포했다.
- 토스 시트 검증 거래 1건을 비고 `콩콩 추가 검증`으로 추가해 사용자 `콩콩` 자동 분류를 확인한 뒤, 비고를 `지니 수정 검증`으로 수정했다. 최신 시트 재조회 결과 웹 목록에서 사용자 태그가 `지니`로 바뀌어 G열 배열 수식이 수정 후에도 유지됨을 확인했다.
- 검증 거래는 삭제까지 실행했고, 웹 목록과 토스 운영원본을 재조회해 `H열삭제 검증` 항목의 잔여 행이 없음을 확인했다.
